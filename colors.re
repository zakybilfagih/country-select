module Light = {
  let textPrimary = `hex("333333");
  let textSecondary = `rgba((0, 0, 0, `percent(52.)));
  let textTertiary = `rgba((0, 0, 0, `percent(32.)));

  let backgroundBox = `hex("FFFFFF");
  let backgroundControlHover = `rgba((46, 60, 86, `percent(7.)));
  let backgroundSelected = `hex("FFDBB3");

  let borderLineAlpha = `rgba((0, 0, 0, `percent(8.)));
  let borderControlAlpha = `rgba((0, 0, 0, `percent(20.)));
  let borderFloatingAlpha = `rgba((0, 0, 0, `percent(10.)));
};
